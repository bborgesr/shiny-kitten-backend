Run `node index.js` to start the server in the development mode.

This repo is the backend of a ToDo Manager app. The frontend is available at [shiny-kitten](https://github.com/bborgesr/shiny-kitten).